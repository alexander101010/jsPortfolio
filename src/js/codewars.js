const scrapeTarget =
  'https://www.codewars.com/users/alexander101010/completed_solutions';

// function ipToInt32(ip) {
//   const binaries = ip
//     .split('.')
//     .map(strNum => Number(strNum).toString(2).padStart(8, '0'));

//   return parseInt(binaries.join(''), 2);
// }
